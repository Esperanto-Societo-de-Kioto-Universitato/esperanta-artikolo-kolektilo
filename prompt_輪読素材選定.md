# エスペラント記事収集・輪読素材選定プロンプト

> **用途**: 生成AI（Claude Code, Cursor, Cline, GitHub Copilot 等のコーディングエージェント）に与え、
> エスペラント記事の収集から輪読会素材の選定までを一貫して実行させる。
>
> **前提**: プロジェクトルートで実行すること。`.venv/bin/python` が使用可能であること。
> 収集済みの記事は `取得文書ekde*/` フォルダ（コーパス）にある。新しく集めるのはコーパスにない期間だけで、
> 手順は README「定期取得の手順」と同じ（重複区間を付けて staging に出し、URL で突合してから振り分ける）。

---

## プロンプト本文

```
あなたは、エスペラント記事を収集・評価・選定するアシスタントです。
以下のステップを順番に実行してください。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ STEP 0: 準備
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. README.md を読み、プロジェクト構成を把握する。特に「記事コレクションの運用記録」
   （コレクションの状態・Monato 収集の仕様と対策・定期取得の手順）を読むこと
2. 既存コーパス（取得文書ekde*/ 。_staging は除く）の期間を README「コレクションの状態」と
   各フォルダの MANIFEST.md で確認し、選定対象期間 {{SELECT_START}} 〜 {{SELECT_END}} のうち
   コーパスにない期間を調べる
   - すべてコーパスにあれば、STEP 1〜1.6 は行わず STEP 2 に進む（取り直しはしない）
   - 足りなければ、STEP 1 の収集期間 {{START_DATE}} 〜 {{END_DATE}} を決める。
     開始日は既存コーパスの最終日の約1か月半前（重複区間。前回実行後に遅れて公開された記事の
     回収と既存記事との突合に使う）、終了日は通常は今日
3. nproc でCPUコア数を確認し、{{WORKERS}} の値を決定する
   （コア数以下を推奨。デフォルト: 8）
4. 出力先「{{OUTPUT_DIR}}」（取得文書ekde<新期間の開始日>_staging。新期間の開始日は
   既存コーパスの最終日の翌日）を作成する。既存の 取得文書ekde*/ フォルダには直接出力しないこと
5. .venv/bin/python が動作することを確認する

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ STEP 1: 6サイトから記事を収集
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
以下6サイトの parallel_scraper.py を使い、期間 {{START_DATE}} 〜 {{END_DATE}} の記事を
{{OUTPUT_DIR}} に収集する。全サイトを **並列** にバックグラウンドで同時実行すること。

Pola Retradio は方針により新規収集しない（誤字脱字が多く整形が大変なため。
既存データは 取得文書ekde20260303 のみ）。"Pola Retradio/parallel_scraper.py" は実行しないこと。

【重要】サイトごとに CLI 引数の対応が異なる。下表を厳守すること：

| # | サイト           | スクリプトパス                                   | --method     | --throttle | 非対応引数                              |
|---|------------------|--------------------------------------------------|--------------|------------|-----------------------------------------|
| 1 | Global Voices    | "Global Voices en Esperanto/parallel_scraper.py"  | auto         | 1.0        | （全引数対応）                          |
| 2 | Libera Folio     | "Libera Folio/parallel_scraper.py"                | rest         | 0.5        | --include-audio                         |
| 3 | Monato           | "Monato/parallel_scraper.py"                      | both ※1※3    | 1.0        | --feed-url (--max-pages/--include-audio は無効) |
| 4 | Scivolemo        | "Scivolemo/parallel_scraper.py"                   | feed         | 1.0        | （全引数対応）                          |
| 5 | UEA Facila       | "Uea_Facila/parallel_scraper.py"                  | （指定不可） | 0.5 ※2     | --method, --include-audio, --feed-url   |
| 6 | El Popola Ĉinio  | "El Popola Ĉinio/parallel_scraper.py"            | feed ※1      | 1.0        | --feed-url (--include-audio は無効)     |

※1 Monato と El Popola Ĉinio は独自ライブラリ（monato_lib / elpopola_lib）を使用。
   El Popola Ĉinio では --method の値は内部で実質無視される（feed を指定しておく）。
   Monato は 2026-08-13 から --method both が既定で、feed（Nova!ページ）に加えて
   publika 連番 ID プローブでの回収を併用する。feed 単独指定は禁物（下記※3）。
※2 UEA Facila のコードデフォルトは 0.5 秒。サーバー負荷を下げたい場合は --throttle 1.0 に上げてもよい。
※3 Monato のフィード（Nova!ページ）は直近1〜2ヶ月分のみ収録。--method feed 単独だと
   1年分を指定しても年間約20本しか取れない。--method both なら ID プローブが過去分を
   回収し、publika 記事を月あたり約15〜30本取得できる（年間150〜250本程度）。

【通常サイト（#1〜#4, #6）の実行コマンド】

  .venv/bin/python "<スクリプトパス>" \
    --start {{START_DATE}} --end {{END_DATE}} \
    --workers {{WORKERS}} \
    --method <上表の値> \
    --throttle <上表の値> \
    --split-by month \
    --out "{{OUTPUT_DIR}}" \
    2>&1

【UEA Facila（#5）専用コマンド】（--method を付けない）

  .venv/bin/python "Uea_Facila/parallel_scraper.py" \
    --start {{START_DATE}} --end {{END_DATE}} \
    --workers {{WORKERS}} \
    --throttle 0.5 \
    --split-by month \
    --out "{{OUTPUT_DIR}}" \
    2>&1

クラスタで qsub を使う場合は、jobs/qsub_<site>_<開始>_<終了>_8w.sh を日付を変えて複製し
（例: jobs/qsub_*_20260701_20260914_8w.sh と submit_all_sites_20260701_20260914_8w.sh）、
README「定期取得の手順」2〜4 に従って投入・完了確認する。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ STEP 1.5: 収集結果の検証
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
全サイトの完了後、以下を確認する：

1. 出力ディレクトリの全ファイル一覧を表示する
2. サイト別の取得記事数を集計して報告する
3. 以下のケースを確認し、対処する：
   - 0件のサイトがある場合:
     → Scivolemo は2024年以降ほぼ更新がないため0件は正常。
       それ以外のサイトで0件なら、エラーログを確認し、
       引数を修正して再実行する。
   - exit code が 0 以外の場合:
     → エラーメッセージを読み、原因を特定する。
       よくある原因: 非対応引数の指定（特に UEA Facila）、
       ネットワークタイムアウト、サイトに接続できない（Global Voices の
       `No route to host` など。時間をおいて再実行する）、サイト構造の変更。
   - 取得数が想定より著しく少ない場合:
     → --method を変えて再試行する（例: auto → both）。

【参考: サイト別の年間取得記事数の目安】
  - El Popola Ĉinio: ~450本
  - UEA Facila: ~100本
  - Libera Folio: ~80本
  - Global Voices: ~30本（月によっては0本の月もある）
  - Monato: 月あたり~15〜30本（--method both の場合。年間150〜250本程度。
    ~20本/年しか取れていなければ feed 単独で走っている兆候なので※3を確認）
  - Scivolemo: 0本（2024年以降ほぼ更新なし）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ STEP 1.6: 既存コーパスとの突合・振り分け
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
README「定期取得の手順」5〜6 に従う：

1. {{OUTPUT_DIR}} の記事を既存の 取得文書ekde*/ と URL 単位で突合する
   （フォルダ間で同じ URL が重複しない状態を保つ）
2. 新期間（既存コーパスの最終日の翌日以降）の記事は 取得文書ekde<新期間の開始日>/ に、
   既存フォルダの期間に入る未収録の記事はそのフォルダに振り分ける。
   重複区間で本文が変わっていればサイト側の修正なので、修正後の本文に更新してよい
3. 記事を入れたフォルダごとに `.venv/bin/python gen_manifest.py <フォルダ>` を実行する
   （既存フォルダの備考は引き継がれる。新しいフォルダには、期間・出所ジョブ・サイト別の
   注意点を書いた備考 md を `--notes` で渡す）
4. `./sync_korpuso.sh` はコーパスリポジトリ（公開）に push するので、振り分け結果を
   ユーザーに報告して確認を得てから実行する

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ STEP 2: 全記事の精読と評価
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
取得文書ekde*/（_staging は除く）の月別 .md ファイルから、選定対象期間
{{SELECT_START}} 〜 {{SELECT_END}} の記事を全サイト分読み込み、各記事について以下を抽出する：
  - タイトル / 公開日 / URL / 著者
  - 本文の概要（2〜3文）
  - おおよその語数（本文だけで数える。語注・朗読案内・写真説明・クレジット・URL は除いた概数）
  - テーマ・ジャンル（文化、社会、科学、言語、政治、環境、文学 等）
  - 全文が利用可能か（ペイウォールや切り詰めがないか）

Pola Retradio の既存データ（取得文書ekde20260303 の pola_retradio_*）は候補にしない（方針により除外）。
過去の輪読素材（取得文書ekde*/rondolegada_materialoj_*.md）と輪読候補リスト
（取得文書ekde*/rondolegado_kandidatoj_*.md）に載った記事は、URL で照合して候補にしない。

【効率化のための並列処理】
記事数が多いため、サイトごとに並列で読み込むこと（サブエージェント等を活用）。
具体的には以下のように分担する：
  - 少量サイト（Global Voices, Monato）→ 全記事を精読
  - 中量サイト（Libera Folio, UEA Facila）→ 全記事を精読
  - 大量サイト（El Popola Ĉinio）→ タイトルと冒頭を流し読みし、
    有望な候補（20〜30本）に絞ってから精読

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ STEP 3: 輪読素材の選定（上位 {{TOP_N}} 本）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
以下の選定基準で、全記事の中から上位 {{TOP_N}} 本を選ぶ：

【選定基準】（優先度順）
1. 対象読者との関連性
   → {{TARGET_AUDIENCE}} にとって親しみやすい、または議論を呼ぶ話題か
2. 議論発展性
   → 多角的な視点が出やすく、参加者が意見を述べやすい内容か
3. 適切な分量
   → 1回の輪読会（60〜90分）で扱える量（400〜2,000語が理想。
     それ以上なら「2回に分けて読む価値がある」と明示する）
4. 全文利用可能
   → ペイウォールや途中切れがなく、完全な本文が読めること
5. エスペラントの文体
   → 文法的に正確で読みやすいか。翻訳調すぎないか
6. テーマの多様性
   → 選出された {{TOP_N}} 本全体で、文化・社会・科学・言語・文学等
     のジャンルが偏らないようにする
7. 出典の多様性
   → できれば複数のサイトから選ぶ（必須ではない）
8. 難易度の段階性
   → やさしい記事と挑戦的な記事をバランスよく含める

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ STEP 4: 結果の出力
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
選定結果を以下のフォーマットで出力する：

各記事について：
  順位 / タイトル / 出典サイト名（公開日、著者） / 語数 / 難易度目安
  > 記事の概要（3〜4文で内容を紹介）
  推薦理由:（なぜこの記事が輪読素材としてふさわしいか。2〜3文）
  ファイルパス:（取得文書ekde*/ のフォルダ名とファイル名）

最後に一覧表（順位・タイトル・出典・語数・テーマ）を付ける。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ STEP 5: 全文ドッキングファイルの作成
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
選定した {{TOP_N}} 本の記事について、全文を1つの .md ファイルにまとめる。

ファイル名: rondolegada_materialoj_YYYYMM.md（YYYYMM は実行月）

【構成】
1. ヘッダ（タイトル・選定基準・実行日）
2. 一覧表（順位・タイトル・出典・語数・テーマ）
3. 各記事の詳細評価（STEP 4 の出力と同内容）
4. 各記事の全文（取得文書ekde*/ の .md から該当部分を抽出）

全文の抽出元は 取得文書ekde*/（_staging は除く）内の月別 .md ファイル。
各記事は見出し（## 第N位: タイトル）で区切り、出典・URL・公開日を明記する。
（別形式: 2026-09 は投票用に候補 50 本の一覧・抽出 md・ルビ HTML を作った。tools/rondolegado/ と README 参照）
```

---

## 変数一覧（実行時に置換する）

| 変数名             | 説明                           | 記入例                              | 決め方                                 |
|--------------------|--------------------------------|-------------------------------------|----------------------------------------|
| `{{SELECT_START}}` | 選定対象期間の開始日           | `2025-10-31`                        | 通常は今日の1年前                      |
| `{{SELECT_END}}`   | 選定対象期間の終了日           | `2026-10-31`                        | 通常は今日                             |
| `{{START_DATE}}`   | 新たに収集する期間の開始日     | `2026-08-01`                        | 既存コーパスの最終日の約1か月半前      |
| `{{END_DATE}}`     | 新たに収集する期間の終了日     | `2026-10-31`                        | 通常は今日（= `{{SELECT_END}}`）       |
| `{{OUTPUT_DIR}}`   | 収集結果の一時出力先（staging）| `取得文書ekde20260915_staging`      | `取得文書ekde<既存最終日の翌日>_staging` |
| `{{WORKERS}}`      | 並列ワーカー数                 | `8`                                 | `nproc` の出力値以下にする             |
| `{{TOP_N}}`        | 選定する記事数                 | `7`                                 | 輪読会の回数に合わせる                 |
| `{{TARGET_AUDIENCE}}` | 対象読者の説明              | `日韓のエスペランチスト（輪読会）`  | 読者の属性・関心に応じて記述           |

既存コーパスの最終日は README「コレクションの状態」で確認する（2026-09-15 時点では 2026-09-14）。
既存コーパスが選定対象期間をすべて含むときは、`{{START_DATE}}`・`{{END_DATE}}`・`{{OUTPUT_DIR}}` は使わない。

---

## 使用例

> README.md をよく読んで文脈を深く理解してから、
> 直近1年のエスペラント記事の中から、
> 日韓のエスペランチストの輪読会素材として
> ふさわしい記事を上位10本に絞ってください。
> コーパスにない期間の記事は、README の定期取得の手順どおりに収集してから選んでください。

2026-10-31 に実行し、既存コーパスが 2026-09-14 までの場合の変数：
- `{{SELECT_START}}` = `2025-10-31`
- `{{SELECT_END}}` = `2026-10-31`
- `{{START_DATE}}` = `2026-08-01`（2026-08-01〜09-14 は既存フォルダとの重複区間。突合に使う）
- `{{END_DATE}}` = `2026-10-31`
- `{{OUTPUT_DIR}}` = `取得文書ekde20260915_staging`（突合後、新期間分は `取得文書ekde20260915/` に入れる）
- `{{WORKERS}}` = `8`
- `{{TOP_N}}` = `10`
- `{{TARGET_AUDIENCE}}` = `日韓のエスペランチスト（輪読会）`

---

## 補足: 既知の注意点

### サイト別の注意

1. **Scivolemo** は科学読み物ブログ Scivolemo (WordPress.com)。2024 年以降ほぼ更新がなく、
   期間内の記事が 0 件のことが多い。取得記事が0件でも正常。
2. **Monato** の大半の記事はペイウォール（有料購読者限定）。
   本文が「La cetero de la artikolo estas konsultebla en la sekcio por abonantoj」
   で終わる記事はプレビューのみ。全文公開記事のみ選定対象とする。
   また、フィード（Nova!ページ）は直近1〜2ヶ月分しか収録されない。
   過去分は --method both（既定）の ID プローブが回収するので、年間150〜250本程度
   取れているはず。約20本/年しか無い場合は取得側の設定を疑うこと。
3. **Global Voices** のエスペラント版は記事数が少なく（年間約30本）、月によっては0本の月もある
   （2026-05-14 の記事のあと更新が止まり、2026-08-26 に再開）。取得数が少なくてもエラーとは限らない。
   以前あった段落の重複は修正済みで、既存データも 2026-09-15 に取り直して解消した。
   サイト側がこちらからの接続を拒むことがある（2026-09-15 に `No route to host`）。その場合は時間をおいて再実行する。
4. **UEA Facila** の記事には「Artikoloj」（読み物記事）と「Filmetoj」（動画埋め込み）が
   混在する。輪読素材としては Artikoloj のみを対象とする（Filmetoj の本文は動画の埋め込みリンクのみ）。
5. **El Popola Ĉinio** は記事数が多い（年間400本超）。全記事を精読する必要はなく、
   タイトルと冒頭で有望な候補を絞り込んでから精読すればよい。
   （2025 年の記事末尾に `<span>` タグ付きの関連記事タイトルが混入していた問題は、2026-09-15 に
   取得コードを修正し、既存データからも除去済み）
6. **Pola Retradio** は方針により新規収集しない（誤字脱字が多く整形が大変なため）。
   既存データは 取得文書ekde20260303（2025-03-03〜2026-03-03）のみで、輪読候補にもしない
   （`取得文書ekde20260814/rondolegado_kandidatoj_202609.md` と同じ扱い）。
7. **UEA Facila** のスクレイパーは環境変数 `UEA_FACILA_USER` / `UEA_FACILA_PASS` が
   設定されている場合のみログイン認証を試み、未設定・認証失敗時は警告を出して
   public session で続行する。この場合、会員限定記事（約10%）が取得できない。
   qsub 経由で使う場合は環境変数を export してから投入する（ジョブは `-V` で引き継ぐ）。
   stderr に `WARNING: UEA Facila login credentials were rejected` と出力される。

### オプション: CRI Esperanto

プロジェクトには CRI Esperanto（中国国際放送）のスクレイパーも存在するが、
CLI 体系が他のサイトと完全に異なる（`--since`/`--until` 方式、デフォルト16ワーカー）。
必要に応じて追加収集できるが、El Popola Ĉinio と内容が重複する可能性がある。
コーパス（取得文書ekde*/）には入れていないので、出力先は取得文書* 以外の作業フォルダにする。

  .venv/bin/python -m cri_esperanto.parallel_scraper \
    --since {{START_DATE}} --until {{END_DATE}} \
    --workers {{WORKERS}} \
    --output-dir "<作業フォルダ>" \
    2>&1
